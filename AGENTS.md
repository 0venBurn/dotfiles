# Agent Guide

## Docs

Codebase knowledge lives in `/docs`. Read before observing or implementing.

## Skills

Reusable workflows live in `/agent/skills`. Use as appropriate:

- /observe → before starting any feature
- /test → before implementing
- /implement → after tests exist
- /review → after implementing

## Rules

- Never implement without a task list.
- Always commit after each completed task section.
